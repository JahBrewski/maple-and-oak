%h1 Update payment information

= form_for @subscription do |f|
  = f.hidden_field :plan_id, value: @plan.id
  = f.hidden_field :stripe_card_token

  #credit-card
    = render :partial => 'plans/card_fields', locals: { f: f}
    = f.submit "update"
